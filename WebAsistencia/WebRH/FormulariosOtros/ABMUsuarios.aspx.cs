#region

using System;

#endregion

public partial class FormulariosOtros_ABMUsuarios : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        //WSViaticosSoapClient ws = new WSViaticosSoapClient();
        //Usuario[] usuarios = ws.GetUsuarios();

        //ListItem li;
        //for (int i = 0; i < usuarios.Count; i++)
        //{
        //    li = new ListItem(usuarios[i].NombreDeUsuario, usuarios[i].Id.ToString());
        //    this.ListBox1.Items.Add(li);

        //}
    }
}
